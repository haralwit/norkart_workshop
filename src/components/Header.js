import React from 'react'

const styles = {
    height: '60px',
    width: '100vw',
    padding: '10px',
    textAlign: 'center',
    fontSize: '30px',
  };

const Header = () => {
    return (
        <div style={styles}>
            <header>Hvor er Moholt? Nå i mer enn en type kart!</header>
        </div>
    )
}

export default Header
